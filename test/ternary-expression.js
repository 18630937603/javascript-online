

// let test = 1==1 ? ( 1==0 ? 20 : 25) : ( 1==1 ? 30 : 40)
let test = 1==0 ?  1==0 ? 20 : 25 :  1==1 ? 30 : 40

// 连续的三元表达式匹配规则：每遇到一个问号，入栈，每遇到一个冒号，匹配栈顶问号出栈，根据问号前面的内容决定这部分变成冒号前面还是后面的内容

console.log(test)